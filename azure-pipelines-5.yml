# Starter pipeline



# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

steps:
  
- task: JFrogNuGet@1
  inputs:
    command: 'restore'
    artifactoryConnection: 'baget-nuget'
    targetResolveRepo: 'ganavik-baget-nuget'
    solutionPath: '**/*.sln'
